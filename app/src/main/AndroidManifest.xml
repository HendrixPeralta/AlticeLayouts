<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.alticelayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".linearLayoutHorizontal"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".linearLayoutVertical" />
        <activity android:name=".Relative_Layout" />
        <activity android:name=".tableLayout" />
        <activity android:name=".Frame_Layout" />
        <activity android:name=".List_View_Activity" />
        <activity android:name=".Grid_View"></activity>
    </application>

</manifest>